# Golden fixture resources

Sanitized gateway frame recordings are added here by the dedicated capture-harness objective.
This tracked resource keeps SwiftPM's `Bundle.module` contract valid without substituting
synthetic frames for real captures.
