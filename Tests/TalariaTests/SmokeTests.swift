@testable import Talaria
import XCTest

final class SmokeTests: XCTestCase {
    /// Placeholder proving the iOS unit-test target runs end-to-end.
    /// Real coverage arrives with P2 surfaces.
    func testTargetLinks() {
        XCTAssertTrue(true)
    }
}
