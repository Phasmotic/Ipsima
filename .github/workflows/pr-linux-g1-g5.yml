name: pr-linux-g1-g5
run-name: "Advisory G1–G5 · PR #${{ github.event.pull_request.number }}"

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

concurrency:
  group: pr-linux-g1-g5-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  linux:
    name: Advisory G1–G5
    permissions:
      contents: read
    uses: Phasmotic/Ipsima/.github/workflows/linux-g1-g5-core.yml@58ec9c0a1e0075705f4b5558741df5dda6f64d22
